---
title: Per-workspace rules
---

# Per-workspace rules

Use per-workspace rules when:

- packages have different runtime roots
- packages expose different import boundaries
- one package needs to follow another workspace package but others do not

This keeps failures actionable and reduces noise from over-broad graph scope.
